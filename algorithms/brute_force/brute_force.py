import time
import itertools
import copy

def brute_force(board, time_limit=10.0):
    print("Uruchamianie algorytmu Brute Force...")

    # Algorytm Pełnego Przeglądu
    solution = [
        ['-', '-', '#'],
        ['B', '4', 'B'],
        ['-', 'B', '-'],
    ]

    print("Algorytm zakończył pracę.")

    return solution
